table "staging"."film_cast" {
  primaryKey = {
    columns = [
      "film_id"
      "person_id"
    ]
    name = "film_cast_pk"
    objectType = primaryKey
  }
  owner = postgres
  columns = (
    column "film_id" {
      type = bigint
      null = NOT NULL
    }
    column "person_id" {
      type = bigint
      null = NOT NULL
    }
    column "film_character" {
      type = text
      null = NOT NULL
    }
  )
}

